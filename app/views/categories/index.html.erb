<div class="categories-hero">
    <h2>CATEGORIES</h2>
</div>

<div class="main">
  <div class="container">
    <% @categories.each do |category| %>
    <div class="category">
      <h3><%= category.name %></h3>
      <%= link_to "Learn more", category_path(category) %>
    </div>
    <% end %>
  </div>
</div>
